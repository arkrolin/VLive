# Hetero 层重构 — 主动运动 / 被动物理 / 非运动 三层拆分

- 文档类型:设计方案 + 全量统计固化
- 日期:2026-07-17
- 状态:统计已完成(数据支撑),重构方案待评审
- 起因:核心 21 维只覆盖头/脸/躯干朝向,**手臂、手、腿等主动运动自由度全部落在
  hetero 层**,与头发、裙摆、PS 残留、道具混在同一个 FSQ blob 里无差别压缩。

---

## 0. 一句话结论

`hetero_latent_raw` 不是"被动物理为主"的杂项层,而是**手臂(28%)+ 面部精细(22%)
两大主动运动组占了一半以上**,却和 ~38% 的非运动参数(特效/道具/PS 残留)混压。
建议把 hetero 按功能三分,让主动运动被单独、优先建模。

---

## 1. 为什么手臂进不了核心 21 维(全量核实,328 机型)

核心集是**按跨机型频率 ≥90% 数据驱动选出**的(`semantic_schema.json._meta.core_threshold=0.9`),
不是按重要性。手臂完全缺席核心,三个数据原因:

**命名极度分裂**
- 481 种手臂/手命名变体;最高频单个 id `PARAM_HAND_L` 仅 60.7%,过不了门槛。
- 头部 `ANGLE_X` 是 Cubism 官方标准名,几乎每个机型同名 → 自然 ≥90%。
  **核心集偏向标准化程度高的参数,手臂恰是标准化最差的。**

**语义并集覆盖率(该机型有无"任意一个"该部位控制参数)**

| 部位 | 覆盖率 |
|---|---|
| 左臂(任意命名) | 86.0% |
| 右臂 | 85.7% |
| 左手 | 88.4% |
| 右手 | 86.9% |
| **任意手臂/手** | **91.8%** |
| 肩 | 33.2% |
| 手指 | 22.0% |

**共存 → 不能靠合并凑覆盖率**
- `ARM_L` 与 `LEFT_ARM` 在 **166 机型共存**(onlyA=4, onlyB=0)→ 不是别名,是成对协作图层。
- `ARM_L` 与 `ARM_L_02` / `ARM_L_TOUSHI` 同样大量共存。
- **每机型手臂参数中位数 = 14 个**(最多 37),是一束协作图层,不是单一自由度。

**根因**:手臂的关节分解是**机型私有**的(每个作者拆图层的方式不同),不像头/脸有
Cubism 官方标准。别名表补不齐,硬塞进对齐 core 会有损且语义不稳。

---

## 2. hetero 层功能构成(294 分片,22969 列,物理过滤后真实入 hetero)

| 功能桶 | 占比 | 性质 |
|---|---|---|
| **手臂/手/手指** | **28.1%** | 主动运动 ← 最大动作组 |
| 面部精细(超出 core 的眼/嘴/眉) | 22.0% | 主动表情 |
| 腿/脚 | 2.6% | 主动运动 |
| 躯干物理(wave/breast/fat) | 2.9% | 半被动 |
| 全局位移(Total/BodyZ) | 1.2% | 根变换 |
| 头发 | 3.0% | 被动物理 |
| 衣物/裙 | 1.7% | 被动物理 |
| 外观特效(黑线/阴影/star/猫耳) | 5.7% | 非运动 |
| 情绪宏(ANGRY/SHY 单参触发整套表情) | 2.0% | 非运动 / 与文本标签语义重叠 |
| 道具(枪/物品) | 6.7% | 非运动 |
| 长尾私有(PSD/ROUGH/Part/OtherType) | 24.0% | 非运动 / 噪声 |

**两个被数据推翻的直觉**
1. hetero 不是被动物理为主 —— 头发+衣物仅 4.7%;真正大头是**手臂 28% + 面部精细 22%**
   两个主动运动组。
2. hetero 里约 **38% 根本不是动作**(特效 5.7% + 宏 2% + 道具 6.7% + 长尾 24%)。让 FSQ
   去学 `PSD`/`ROUGH`/`STAR`/`RED` 的"运动"是浪费码本容量。

---

## 3. 重构方案 — 三层拆分

```
core 21 维        ← 跨机型标准化连续量(头/脸/躯干朝向)。现状保留,不动。
─────────────────────────────────────────────────────────────
hetero-A 主动运动 ← 手臂 28% + 腿 2.6% + 面部精细 22% + 全局 1.2%   (~54%)
                    这层才是"具身动作"。应让 Stage2 优先 / 单独建模。
hetero-B 被动物理 ← 头发 3% + 衣物 1.7% + 躯干物理 2.9%            (~7.6%)
                    可由物理引擎 / FSQ 兜底,建模优先级低。
hetero-C 非运动   ← 特效 5.7% + 宏 2% + 道具 6.7% + 长尾 24%        (~38%)
                    考虑从动作生成目标剔除或旁路处理,减轻 FSQ 负担。
```

**设计立场**
- 核心 21 维定义保持纯粹:"跨机型标准化的连续动作量",**不把机型私有的手臂硬塞进来**。
- 手臂等主动运动从"和头发/PS残留同层"里**提级**,标为 hetero-A 单独建模。
- 尊重手臂关节分解的机型私有性 —— 仍按机型走 FSQ,但 A/B/C 分组后可分别压缩 /
  分别设码本 / 让 Stage2 对 A 组加权。
- 非运动参数(C 组)清出动作目标,是纯收益。

**尚待决策(重训前须定)**
- A/B/C 是否各用独立 FSQ 码本,还是共享码本 + 分组权重?
- C 组是完全剔除,还是保留原值旁路(渲染回放仍需要)?
- 面部精细(22%)归 A 组还是与 core 表情合并考虑?

---

## 4. 分类器与标签落盘

工具:`tools/classify_hetero.py`(见该文件)。

- 输入:`out/shards/*.npz` 的 `hetero_ids`。
- 输出:`hetero_labels.json` —— 每个机型每列打 `A_active` / `B_passive` / `C_nonmotion`
  标签 + 细分桶(limb/face/hair/cloth/fx/macro/prop/tail…)。
- 用途:重训时按标签分组;审计分类边界;可迭代规则(拼音/长尾覆盖不完整,规则可增补)。
- 分类基于**参数 id 正则**(含拼音:`SHIZHI`食指/`SHOUWAN`手腕/`TUI`腿 等),first-match-wins,
  未命中归 `C_nonmotion.tail`(机型私有长尾,3026 种,符合 hetero+FSQ 吸收定位)。

---

## 附:统计复现

```bash
# 全量命名/共存(用 model_params.json,覆盖全 328 机型)
uv run python tools/classify_hetero.py --stats

# 打标签落盘(用 out/shards/*.npz,物理过滤后真实入 hetero 的列)
uv run python tools/classify_hetero.py --label --out hetero_labels.json
```
