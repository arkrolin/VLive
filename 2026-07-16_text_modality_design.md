# Live2D-VLA 文本模态支持设计

- 文档类型:设计方案(companion to `2026-07-16_model_architecture_full_spec.md`)
- 日期:2026-07-16
- 状态:方案待评审,尚未实现
- 目标:在现有「视频→21维动作」扩散模型上,增加「文本→21维动作」能力

---

## 0. 核心原则(先立规矩)

**文本给意图,扩散填实现。**

- 文本描述停在**语义/意图层**("生气"、"惊讶地后退"),不下探到参数级。
- 同一句文本对应**多种合理动作**,由扩散采样产生 —— 这是扩散模型的核心价值(一对多生成)。
- 若把文本精确到窗口级参数轨迹("ANGLE_X 0→-0.5、眯眼、张嘴"),等于把生成退化为
  确定性回归,榨干扩散的自由度和多样性。**明确不做窗口级参数反推描述。**


"当前数据集为视频→动作,**无文本标注**"。**此判断已过时**:数据集已有 `text_prompts`
字段(动作级,74.5% 覆盖)。瓶颈不是"有没有文本",是"文本够不够丰富"(层次 4)。
